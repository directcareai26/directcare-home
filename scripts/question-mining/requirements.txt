# Google Search Console (real question-demand source)
google-api-python-client>=2.0
google-auth>=2.0
google-auth-oauthlib>=1.0
# Claude — clustering / scoring / compliant draft specs
anthropic>=0.40
# (Autocomplete + Reddit use only the Python stdlib — no extra deps)
